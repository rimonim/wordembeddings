% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_glove.R
\name{train_glove}
\alias{train_glove}
\title{Train embeddings with GloVe}
\usage{
train_glove(
  fcm,
  n_dims = 100,
  x_max = 100,
  alpha = 0.75,
  lr = 0.05,
  epochs = 15,
  weight_fn = c("glove", "power", "log", "uniform"),
  fix_bias = FALSE,
  output = c("word_embeddings", "context_embeddings", "all"),
  init = c("uniform", "normal"),
  shuffle = TRUE,
  seed = NULL,
  verbose = TRUE,
  threads = parallel::detectCores()
)
}
\arguments{
\item{fcm}{a \link[quanteda:fcm]{Quanteda fcm} or similar 2D matrix-like or 3D
array-like object containing co-occurrence counts.}

\item{n_dims}{integer. Dimensionality of embeddings.}

\item{x_max}{numeric. Maximum co-occurrence count for weighting function.
Co-occurrences above this value receive weight 1.0.}

\item{alpha}{numeric. Exponent for the weighting function. Default is 0.75
as in the original GloVe paper.}

\item{lr}{numeric. Learning rate for AdaGrad optimizer. Default is 0.05.}

\item{epochs}{integer. Number of training epochs. Default is 15.}

\item{weight_fn}{character. Weighting function type:
\describe{
\item{"glove"}{(default) Original GloVe weighting: \code{f(x) = (x/x_max)^alpha}
if \code{x < x_max}, else 1.}
\item{"power"}{Power weighting: \code{f(x) = x^alpha} (no cap).}
\item{"log"}{Log weighting: \code{f(x) = min(1, log(x)/log(x_max))}.}
\item{"uniform"}{Uniform weighting: \code{f(x) = 1}.}
}}

\item{fix_bias}{logical. If \code{TRUE}, fix the word and context biases at
\code{log(marginal_count)} rather than learning them. This can be useful when
you want the bias terms to exactly capture marginal frequencies.}

\item{output}{character. Which embeddings to include in the output:
\describe{
\item{"word_embeddings"}{(default) Include only word embeddings.}
\item{"context_embeddings"}{Include only context embeddings.}
\item{"all"}{Include both word and context embeddings.}
}}

\item{init}{character. Initialization method for embeddings:
\describe{
\item{"uniform"}{(default) Uniform random in \verb{[-0.5/n_dims, 0.5/n_dims]}.}
\item{"normal"}{Normal random with mean 0 and sd 0.01.}
}}

\item{shuffle}{logical. Whether to shuffle co-occurrence pairs each epoch.
Default is \code{TRUE}.}

\item{seed}{integer. Random seed for reproducibility.}

\item{verbose}{logical. Print progress information.}

\item{threads}{integer. Number of threads. Default uses all available cores.}
}
\value{
A list containing:
\describe{
\item{word_embeddings}{Matrix of word embeddings (nrow(fcm) x n_dims),
if requested.}
\item{context_embeddings}{Matrix of context embeddings (ncol(fcm) x n_dims),
if requested.}
\item{bias_i}{Word bias terms.}
\item{bias_j}{Context bias terms.}
\item{cost_history}{Loss value at each epoch.}
}
}
\description{
\code{train_glove()} trains word and context embeddings using the GloVe
(Global Vectors) algorithm. GloVe is a weighted matrix factorization method
that learns embeddings by factorizing the log co-occurrence matrix.
}
\examples{
\dontrun{
# Create a simple co-occurrence matrix
library(quanteda)
toks <- tokens(c("the cat sat on the mat", "the dog ran in the park"))
fcm_mat <- fcm(toks, context = "window", window = 2)

# Train GloVe embeddings
result <- train_glove(fcm_mat, n_dims = 50, epochs = 10)

# Access embeddings
word_emb <- result$word_embeddings
}

}
\references{
Pennington, J., Socher, R., & Manning, C. D. (2014). GloVe: Global Vectors
for Word Representation. In Proceedings of the 2014 Conference on Empirical
Methods in Natural Language Processing (EMNLP) (pp. 1532-1543).
}
